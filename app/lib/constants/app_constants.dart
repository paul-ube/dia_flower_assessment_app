import 'package:flutter/material.dart';

const kEmphasizedAccelerate = Cubic(0.3, 0.0, 0.8, 0.15);
const kEmphasizedDecelerate = Cubic(0.05, 0.7, 0.1, 1.0);
const kStandardAccelerate = Cubic(0.3, 0, 1, 1);
const kStandardDecelerate = Cubic(0, 0, 0, 1);

const double kMinimumButtonHeight = 56;
const double kDefaultAvatarRadius = 24;

const double kIndent = 20;

const double m3ToolbarHeight = 64;
const double kProductItemPadding = 6;
const double kProductsHorizontalPadding = kIndent - kProductItemPadding;

const double kProductImageExpandedHeight = 400;
const double kProductImageBottomContainerHeight = 25;
const double kSectionHeaderHeight = 56;

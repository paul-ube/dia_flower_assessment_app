import 'package:flutter/material.dart';

class AppColors {
  static Color onImageTextBackgroundColor = Color(
    0xff0b0f0a,
  ).withValues(alpha: 0.7);
  static Color onImageTextColor = Colors.white;
}
